mixin styleguide-header(data, options)
  .styleguide-header(class=attributes.class)
    if data.logo
      img(src=data.logo, alt=data.company + ' logo')
    else
      h2.styleguide-company-brand #{data.company}

    p.styleguide-title styleguide
